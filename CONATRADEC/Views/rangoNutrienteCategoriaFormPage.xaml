<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CONATRADEC.Views.rangoNutrienteCategoriaFormPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Shell.NavBarIsVisible="False"
    BackgroundColor="{AppThemeBinding Light={StaticResource SurfacePage}, Dark={StaticResource SurfacePageDark}}">

    <Grid>
        <ScrollView
            HorizontalScrollBarVisibility="Never"
            VerticalScrollBarVisibility="Default">
            <VerticalStackLayout
                x:Name="FormularioContainer"
                MaximumWidthRequest="920"
                HorizontalOptions="Center"
                Padding="{OnIdiom Phone='14,14,14,24', Tablet='22,18,22,28', Desktop='28,22,28,32'}"
                Spacing="14">

                <Border Style="{StaticResource ModernHeroCard}">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="13">
                        <Border
                            Style="{StaticResource ModernIconTile}"
                            WidthRequest="52"
                            HeightRequest="52"
                            VerticalOptions="Start">
                            <Image
                                Source="iconcultivo.png"
                                WidthRequest="28"
                                HeightRequest="28"
                                HorizontalOptions="Center"
                                VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="3" VerticalOptions="Center">
                            <Label Text="{Binding Title}" Style="{StaticResource ModernPageTitle}" />
                            <Label
                                Text="Este cultivo agrupa los rangos nutricionales utilizados por el sistema."
                                Style="{StaticResource ModernPageSubtitle}" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>

                <Border Style="{StaticResource ModernFormCard}">
                    <VerticalStackLayout Spacing="15">
                        <VerticalStackLayout Spacing="3">
                            <Label Text="Información del cultivo" Style="{StaticResource ModernSectionTitle}" />
                            <Label
                                Text="El mismo catálogo se reutiliza en análisis de suelo y parámetros nutricionales."
                                Style="{StaticResource ModernCaption}" />
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="6">
                            <Label Text="Nombre del tipo de cultivo" Style="{StaticResource ModernFieldLabel}" />
                            <Border Style="{StaticResource ModernInputBorder}">
                                <Border.Triggers>
                                    <DataTrigger TargetType="Border" Binding="{Binding TieneErrorNombre}" Value="True">
                                        <Setter Property="Stroke" Value="{StaticResource RedDelete}" />
                                        <Setter Property="StrokeThickness" Value="2" />
                                    </DataTrigger>
                                </Border.Triggers>
                                <Entry
                                    Style="{StaticResource ModernEntry}"
                                    Text="{Binding Nombre}"
                                    Placeholder="Ejemplo: CAFÉ"
                                    MaxLength="80"
                                    IsReadOnly="{Binding IsReadOnly}" />
                            </Border>
                            <Label
                                Text="{Binding ErrorNombre}"
                                IsVisible="{Binding TieneErrorNombre}"
                                FontSize="11"
                                TextColor="{StaticResource RedDelete}" />
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="6">
                            <Label Text="Descripción" Style="{StaticResource ModernFieldLabel}" />
                            <Border
                                Style="{StaticResource ModernInputBorder}"
                                MinimumHeightRequest="124"
                                Padding="10,7">
                                <Border.Triggers>
                                    <DataTrigger TargetType="Border" Binding="{Binding TieneErrorDescripcion}" Value="True">
                                        <Setter Property="Stroke" Value="{StaticResource RedDelete}" />
                                        <Setter Property="StrokeThickness" Value="2" />
                                    </DataTrigger>
                                </Border.Triggers>
                                <Editor
                                    Text="{Binding Descripcion}"
                                    Placeholder="Descripción del tipo de cultivo"
                                    MaxLength="150"
                                    AutoSize="TextChanges"
                                    MinimumHeightRequest="106"
                                    IsReadOnly="{Binding IsReadOnly}"
                                    BackgroundColor="Transparent"
                                    TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}"
                                    PlaceholderColor="{AppThemeBinding Light={StaticResource TextTertiary}, Dark={StaticResource TextTertiaryDark}}"
                                    FontFamily="MontserratMedium"
                                    FontSize="13" />
                            </Border>
                            <Label Text="Máximo 150 caracteres." Style="{StaticResource ModernCaption}" />
                            <Label
                                Text="{Binding ErrorDescripcion}"
                                IsVisible="{Binding TieneErrorDescripcion}"
                                FontSize="11"
                                TextColor="{StaticResource RedDelete}" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <Grid
                    ColumnDefinitions="{OnIdiom Phone='*', Tablet='*,*', Desktop='*,*'}"
                    RowDefinitions="Auto,Auto"
                    ColumnSpacing="10"
                    RowSpacing="9"
                    Margin="0,2,0,12">
                    <Button
                        Text="Guardar"
                        Style="{StaticResource ModernPrimaryButton}"
                        Command="{Binding SaveCommand}"
                        IsVisible="{Binding ShowSaveButton}"
                        IsEnabled="{Binding NotIsBusy}" />
                    <Button
                        Grid.Column="{OnIdiom Phone=0, Tablet=1, Desktop=1}"
                        Grid.Row="{OnIdiom Phone=1, Tablet=0, Desktop=0}"
                        Text="Cancelar"
                        Style="{StaticResource ModernNeutralButton}"
                        Command="{Binding CancelCommand}"
                        IsVisible="{Binding IsEditable}"
                        IsEnabled="{Binding NotIsBusy}" />
                    <Button
                        Grid.Column="0"
                        Grid.ColumnSpan="{OnIdiom Phone=1, Tablet=2, Desktop=2}"
                        Text="Regresar"
                        Style="{StaticResource ModernNeutralButton}"
                        Command="{Binding CancelCommand}"
                        IsVisible="{Binding IsReadOnly}"
                        IsEnabled="{Binding NotIsBusy}" />
                </Grid>
            </VerticalStackLayout>
        </ScrollView>

        <Grid
            ZIndex="100"
            IsVisible="{Binding IsBusy}"
            InputTransparent="False"
            BackgroundColor="#28000000">
            <Border
                Padding="18,14"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceCard}, Dark={StaticResource SurfaceCardDark}}"
                Stroke="{AppThemeBinding Light={StaticResource BorderSoft}, Dark={StaticResource BorderSoftDark}}"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 14">
                <HorizontalStackLayout Spacing="10">
                    <ActivityIndicator
                        IsRunning="{Binding IsBusy}"
                        Color="{StaticResource color_Verde}"
                        WidthRequest="24"
                        HeightRequest="24" />
                    <Label
                        Text="Procesando cultivo..."
                        VerticalOptions="Center"
                        FontFamily="MontserratBold"
                        FontSize="12"
                        TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                </HorizontalStackLayout>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
