<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CONATRADEC.Views.albumFotoVisorPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:CONATRADEC.Controls"
    xmlns:converters="clr-namespace:CONATRADEC.Converters"
    Shell.NavBarIsVisible="False"
    BackgroundColor="#090B0A">

    <ContentPage.Resources>
        <converters:AlbumImageSourceConverter x:Key="AlbumImageSourceConverter" />
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*,Auto">

        <Grid
            Grid.Row="0"
            Padding="{OnPlatform WinUI='22,16', Default='14,10'}"
            ColumnDefinitions="Auto,*,Auto"
            ColumnSpacing="12"
            BackgroundColor="#DD090B0A">

            <Button
                Grid.Column="0"
                Text="✕"
                Command="{Binding CerrarCommand}"
                WidthRequest="46"
                HeightRequest="46"
                Padding="0"
                CornerRadius="23"
                BackgroundColor="#26FFFFFF"
                TextColor="White"
                FontSize="18"
                FontAttributes="Bold"
                SemanticProperties.Description="Cerrar visor" />

            <Label
                Grid.Column="1"
                Text="{Binding TituloAlbum}"
                VerticalOptions="Center"
                FontFamily="MontserratBold"
                FontSize="{OnPlatform WinUI=18, Default=15}"
                FontAttributes="Bold"
                TextColor="White"
                LineBreakMode="TailTruncation" />

            <Border
                Grid.Column="2"
                Padding="11,6"
                VerticalOptions="Center"
                BackgroundColor="#26FFFFFF"
                StrokeThickness="0"
                StrokeShape="RoundRectangle 12">

                <Label
                    Text="{Binding ContadorTexto}"
                    FontSize="12"
                    FontAttributes="Bold"
                    TextColor="White" />
            </Border>
        </Grid>

        <Grid Grid.Row="1">
            <!--
                Se conserva el swipe nativo del CarouselView. En Android el
                control de imagen bloquea la intercepción del carrusel solo
                durante un pellizco o mientras la fotografía está ampliada.
            -->
            <CarouselView
                x:Name="FotosCarousel"
                ItemsSource="{Binding Fotos}"
                CurrentItem="{Binding FotoSeleccionada, Mode=TwoWay}"
                IndicatorView="FotosIndicator"
                Loop="False"
                IsSwipeEnabled="True"
                IsBounceEnabled="False"
                HorizontalOptions="Fill"
                VerticalOptions="Fill">

                <CarouselView.ItemTemplate>
                    <DataTemplate>
                        <Grid
                            Padding="{OnPlatform WinUI='34,20', Default='8,12'}">

                            <!--
                                El visor utiliza primero la fotografía original.
                                Si no está disponible localmente, utiliza la
                                miniatura descargada como respaldo.
                            -->
                            <controls:ZoomableAlbumImage
                                Source="{Binding FotoVisorUrl, Converter={StaticResource AlbumImageSourceConverter}}" />

                            <Border
                                Margin="12"
                                Padding="9,5"
                                HorizontalOptions="End"
                                VerticalOptions="Start"
                                IsVisible="{Binding EsPortada}"
                                BackgroundColor="{StaticResource color_Verde}"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 10">

                                <Label
                                    Text="Portada"
                                    FontSize="11"
                                    FontAttributes="Bold"
                                    TextColor="White" />
                            </Border>
                        </Grid>
                    </DataTemplate>
                </CarouselView.ItemTemplate>
            </CarouselView>

            <IndicatorView
                x:Name="FotosIndicator"
                Margin="0,0,0,12"
                HorizontalOptions="Center"
                VerticalOptions="End"
                IndicatorColor="#66FFFFFF"
                SelectedIndicatorColor="White" />
        </Grid>

        <Border
            Grid.Row="2"
            Margin="{OnPlatform WinUI='22,8,22,18', Default='12,8,12,14'}"
            Padding="14,11"
            BackgroundColor="#D91B1E1C"
            Stroke="#33FFFFFF"
            StrokeThickness="1"
            StrokeShape="RoundRectangle 14">

            <VerticalStackLayout Spacing="4">
                <Label
                    Text="{Binding DescripcionTexto}"
                    FontSize="12"
                    TextColor="White"
                    HorizontalTextAlignment="Center"
                    LineBreakMode="WordWrap" />

                <Label
                    Text="Pellizque con dos dedos para ampliar · Con zoom arrastre para mover · Doble toque para ampliar/restablecer"
                    FontSize="10"
                    TextColor="#BFD0CA"
                    HorizontalTextAlignment="Center"
                    LineBreakMode="WordWrap" />
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>
