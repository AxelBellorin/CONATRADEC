<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sys="clr-namespace:System;assembly=netstandard"
             x:Class="CONATRADEC.Views.userFormPage"
             Title="Formulario de usuario"
             Shell.NavBarIsVisible="False"
             Background="Transparent">

    <Grid RowDefinitions="Auto,*">

        <Label Grid.Row="0"
               Text="{Binding Title}"
               TextColor="{StaticResource color_Verde}"
               FontSize="24"
               FontAttributes="Bold"
               Margin="10,20,10,5" />

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="10"
                                 Spacing="15">

                <Border Style="{StaticResource BorderStyle}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Usuario"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />
                        <Entry Placeholder="Nombre de usuario"
                               Style="{StaticResource EntryStyle}"
                               Text="{Binding NombreUsuario}"
                               IsReadOnly="{Binding IsUserNameReadOnly}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource BorderStyle}"
                        IsVisible="{Binding ShowPasswordField}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Contraseña"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />

                        <Grid ColumnDefinitions="*,Auto">
                            <Entry Grid.Column="0"
                                   Style="{StaticResource EntryStyle}"
                                   Placeholder="{Binding PasswordPlaceholder}"
                                   Text="{Binding ClaveUsuario}"
                                   Keyboard="Password"
                                   IsPassword="{Binding IsPasswordHidden}"
                                   Background="Transparent" />

                            <ImageButton Grid.Column="1"
                                         Margin="10,0,0,0"
                                         Source="{Binding PasswordToggleIcon}"
                                         Command="{Binding TogglePasswordCommand}"
                                         BackgroundColor="Transparent"
                                         WidthRequest="30"
                                         HeightRequest="30" />
                        </Grid>

                        <Label Text="{Binding PasswordHelpText}"
                               FontSize="12"
                               TextColor="{StaticResource color_Gris}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource BorderStyle}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Nombre completo"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />
                        <Entry Placeholder="Nombre completo"
                               Style="{StaticResource EntryStyle}"
                               Text="{Binding NombreCompletoUsuario}"
                               IsReadOnly="{Binding IsReadOnly}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource BorderStyle}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Identificación"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />
                        <Entry Placeholder="Ejemplo: 001-080701-1050R"
                               Style="{StaticResource EntryStyle}"
                               Text="{Binding IdentificacionUsuario}"
                               MaxLength="18"
                               IsReadOnly="{Binding IsReadOnly}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource BorderStyle}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Correo electrónico"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />
                        <Entry Placeholder="usuario@dominio.com"
                               Style="{StaticResource EntryStyle}"
                               Text="{Binding CorreoUsuario}"
                               Keyboard="Email"
                               IsReadOnly="{Binding IsReadOnly}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource BorderStyle}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Teléfono"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />
                        <Entry Placeholder="8 dígitos"
                               Style="{StaticResource EntryStyle}"
                               Text="{Binding TelefonoUsuario}"
                               Keyboard="Telephone"
                               MaxLength="8"
                               IsReadOnly="{Binding IsReadOnly}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource BorderStyle}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Fecha de nacimiento"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />
                        <DatePicker Date="{Binding FechaNacimientoDate}"
                                    MinimumDate="1900-01-01"
                                    MaximumDate="{x:Static sys:DateTime.Now}"
                                    Format="dd/MM/yyyy"
                                    IsEnabled="{Binding IsEnabled}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource BorderStyle}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Rol"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />
                        <Picker Title="Seleccione un rol"
                                ItemsSource="{Binding Roles}"
                                ItemDisplayBinding="{Binding NombreRol}"
                                SelectedItem="{Binding RolSeleccionado, Mode=TwoWay}"
                                IsEnabled="{Binding IsEnabled}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource BorderStyle}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="País"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />
                        <Picker Title="Seleccione un país"
                                ItemsSource="{Binding Paises}"
                                ItemDisplayBinding="{Binding NombrePais}"
                                SelectedItem="{Binding PaisSeleccionado, Mode=TwoWay}"
                                IsEnabled="{Binding IsEnabled}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource BorderStyle}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Departamento"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />
                        <Picker Title="Seleccione un departamento"
                                ItemsSource="{Binding Departamentos}"
                                ItemDisplayBinding="{Binding NombreDepartamento}"
                                SelectedItem="{Binding DepartamentoSeleccionado, Mode=TwoWay}"
                                IsEnabled="{Binding CanPickDepartamento}" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource BorderStyle}">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Municipio"
                               FontAttributes="Bold"
                               FontSize="17"
                               TextColor="{StaticResource color_Negro}" />
                        <Picker Title="Seleccione un municipio"
                                ItemsSource="{Binding Municipios}"
                                ItemDisplayBinding="{Binding NombreMunicipio}"
                                SelectedItem="{Binding MunicipioSeleccionado, Mode=TwoWay}"
                                IsEnabled="{Binding CanPickMunicipio}" />
                    </VerticalStackLayout>
                </Border>

                <Button Text="Seleccionar imagen"
                        IsEnabled="{Binding EnabledImagenField}"
                        Style="{StaticResource AddButton}"
                        Command="{Binding SeleccionarImagenCommand}"
                        BackgroundColor="{StaticResource color_Verde}"
                        TextColor="White"
                        Margin="0,10" />

                <Image HeightRequest="120"
                       WidthRequest="120"
                       Aspect="AspectFill"
                       Source="{Binding ImagenPreview}"
                       Margin="0,5"
                       HorizontalOptions="Center" />

                <Grid ColumnDefinitions="*,*"
                      Margin="10,0">
                    <Button Grid.Column="0"
                            Text="Guardar"
                            Style="{StaticResource UpdateButton}"
                            IsVisible="{Binding ShowSaveButton}"
                            IsEnabled="{Binding NotIsBusy}"
                            Command="{Binding SaveCommand}"
                            Margin="10,0" />

                    <Button Grid.Column="1"
                            Text="Cancelar"
                            Style="{StaticResource CancelButton}"
                            IsEnabled="{Binding NotIsBusy}"
                            Command="{Binding CancelCommand}"
                            Margin="10,0" />
                </Grid>

                <ActivityIndicator IsRunning="{Binding IsBusy}"
                                   IsVisible="{Binding IsBusy}"
                                   HorizontalOptions="Center"
                                   Margin="0,10" />

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
